
The code style is primarily determined by the Hydejack theme and Jekyll conventions.

*   **File Naming:** Blog posts are named with the format `YYYY-MM-DD-title.md`.
*   **Front Matter:** Pages and posts use YAML front matter for metadata.
*   **Sass:** Custom styles are written in Sass and located in the `_sass` directory.
*   **JavaScript:** The JavaScript code is located in the `#jekyll-theme-hydejack/_js` directory and is likely written in a modular way.
